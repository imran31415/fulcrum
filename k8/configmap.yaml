apiVersion: v1
kind: ConfigMap
metadata:
  name: fulcrum-config
  namespace: fulcrum
data:
  # Environment variables for the application
  NODE_ENV: "production"
  API_URL: "https://fulcrum.scalebase.io"
  WASM_PATH: "/main.wasm"
  APP_NAME: "Fulcrum"
  APP_VERSION: "1.0.0"
